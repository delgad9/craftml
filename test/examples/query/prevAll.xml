<craft>

    <g></g>
    <g></g>
    <g></g>
    <g></g>
    <cube></cube>

    <script>

        expect($('cube').prevAll()).to.have.length(4)

        $('cube').prevAll().each(function(){
            expect(this).to.have.name('g')
        })

    </script>

</craft>
