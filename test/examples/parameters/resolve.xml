<craft>

    <craft name="foo">
        <parameter name="p" type="int" default="1"/>
        <parameter name="q" type="int" default="1"/>
        <script>
            function main(params, scope){
                expect(params).to.have.property('p').and.eql(2)
                expect(params).to.have.property('q').and.eql(1)
            }
        </script>
    </craft>

    <foo p="2"/>
            
</craft>
