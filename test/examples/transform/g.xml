<craft>

    <g id="outside" t="translate(5 0 0)">
        <g id="inside">
            <cube id="foo" t="translate(-20 0 0)"></cube>
            <cube t="translate(0 0 0)"></cube>
            <cube t="translate(20 0 0)"></cube>
        </g>

        <script>
            // the size of g#inside should be the size of the box containing
            // all of its children
            expect(this.prev).to.have.size(50,10,10).and.location(-20,0,0)

            // the location of the cube#foo should stay the same
            expect(this.prev.children[0]).to.have.location(-20,0,0)
        </script>
    </g>

    <script>
        // the size of g#outside should be as big as g#inside, but it location is
        // moved by 5 along x
        expect(this.prev).to.have.size(50,10,10).and.location(-15,0,0)
    </script>
        
</craft>
