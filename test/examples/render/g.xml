<craft>
    <g>
        <g></g>
        <g></g>
        <g></g>
    </g>
    <g>
    </g>
    <script>
        expect(this.parent).to.have.solid('g').at([0]).that.have.role('group')
        expect(this.parent).to.have.solid('g').at([0,0]).that.have.role('group')
        expect(this.parent).to.have.solid('g').at([0,1]).that.have.role('group')
        expect(this.parent).to.have.solid('g').at([0,2]).that.have.role('group')
        expect(this.parent).to.not.have.solid.at([0,3])

        expect(this.parent).to.have.solid('g').at([0,1]).that.have.role('group')
        expect(this.parent).to.not.have.solid.at([0,1,0])
    </script>
</craft>
