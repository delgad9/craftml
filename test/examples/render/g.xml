<craft>
    <g>
        <g></g>
        <g></g>
        <g></g>
    </g>
    <g>
    </g>
    <script>
        expect(this).to.have.children(['g','g'])
        expect(this.children[0]).to.have.children(['g','g','g'])
        expect(this.children[1]).to.have.children([])
    </script>
</craft>
