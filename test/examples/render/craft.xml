<craft>
    <craft name="foo">
        <g></g>
        <g></g>
    </craft>
    <craft name="bar">
        <g></g>
    </craft>
    <script>
        expect(this.parent).to.have.solid('craft').at([0]).that.have.role('define')
        expect(this.parent).to.have.solid('craft').at([1]).that.have.role('define')
    </script>
    <foo></foo>
    <bar></bar>
    <script>
        // this.pp()

        expect(this.parent).to.have.solid('script').at([2])

        expect(this.parent).to.have.solid('foo').at([3])
            .that.have.children(['g','g'])

        expect(this.parent).to.have.solid('bar').at([4])
            .that.have.children(['g'])
    </script>
</craft>
