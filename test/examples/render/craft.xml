<craft>
    <craft name="foo">
        <g></g>
        <g></g>
    </craft>
    <craft name="bar">
        <g></g>
    </craft>
    <script>
        function main(params, scope){
            expect(this).to.have.solid('craft').at('0').that.have.role('define')
            expect(this).to.have.solid('craft').at('1').that.have.role('define')
        }
    </script>
    <foo></foo>
    <bar></bar>
    <script>
        function main(params, scope){
            // this.pp()

            expect(this).to.have.solid('script').at('2')

            expect(this).to.have.solid('foo').at('3')
                .that.have.children(['g','g'])

            expect(this).to.have.solid('bar').at('4')
                .that.have.children(['g'])
        }
    </script>
</craft>
