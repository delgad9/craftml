<craft>
    <craft name="c1"></craft>
    <craft name="c2"></craft>
    <g></g>
    <cube></cube>
    <script>
        expect(scope.solids).to.have.length(2)
        //expect(scope.solids[0].role).to.be.eql('group')
        //expect(scope.solids[1].role).to.be.eql('group')
        // this.parent.pp()
    </script>

</craft>
