<craft name="group">
    <content></content>
    <script>
        function main(params, scope) {
            var grp = Solid.fromGroup(scope.solids)
            // assign the info object of the current scope to the new
            // group solid
            grp.info = scope.info
            scope.solids = [grp]
        }
    </script>
</craft>
