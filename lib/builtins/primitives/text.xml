<craft name="text">
    <parameter name="text" default="" type="string"/>
    <group transform="scale(0.3,0.3,0.5) mirrorY()">
        <script>
            function main(params, scope) {

                var text = params.text
                var size = 10

                var l = vector_text(0, 0, text); // l contains a list of polylines to be drawn
                var o = [];
                var scaleFactor = 0.25 * (size / 10);
                var depthScaleFactor = 0.1;

                l.forEach(function(pl) { // pl = polyline (not closed)
                    var o = rectangular_extrude(pl, {
                        w: 2,
                        h: 2
                    }) // extrude it to 3D

                    var s = new Solid(o)
                    s.color = 'gray'
                    scope.solids.push(s)
                })
            }
        </script>
    </group>
</craft>
