<craft name="cube">
    <parameter name="xsize" default="10" type="int"/>
    <parameter name="ysize" default="10" type="int"/>
    <parameter name="zsize" default="10" type="int"/>
    <parameter name="size" type="size"/>
    <script type="text/openjscad">
        var size = params.size
        if (size){
            this.add(cube([size.x, size.y, size.z]))
        } else {
            // for backward compatibility
            this.add(cube([params.xsize, params.ysize, params.zsize]))
        }
    </script>
</craft>
