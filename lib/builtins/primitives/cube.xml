<craft name="cube">
    <parameter name="size" type="size" default="10 10 10"/>

    <g l="lineupZ({{size.z}}) solidify(rectangle)" grouping="off">
        <repeat n="2">
            <rectangle width="{{size.x}}" height="{{size.y}}"></rectangle>
        </repeat>
    </g>

</craft>
