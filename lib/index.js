'strict'

var _ = require('lodash')

var lib = {
    parse: require('./parse')
}
// _.merge(lib, require('./parse'))
// _.merge(lib, require('./script'))
// _.merge(lib, require('./craft'))
// _.merge(lib, require('./solid'))

module.exports = lib